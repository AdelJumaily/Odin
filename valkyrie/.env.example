# =============================
# ODIN / VALKYRIE CONFIGURATION
# =============================

# API keys for testing (prod uses users table keys; these help bootstrap/demo)
ODIN_ADMIN_KEY=changeme-admin
ODIN_READER_KEY=changeme-reader

# Where encrypted files live inside the container
FILES_DIR=/files

# Postgres
POSTGRES_USER=valkyrie
POSTGRES_PASSWORD=valkyrie_pass
POSTGRES_DB=valkyrie
DATABASE_URL=postgresql+psycopg://valkyrie:valkyrie_pass@postgres:5432/valkyrie

# 32-byte base64 AES key. Generate a fresh one for .env:
# python - <<<'import os,base64;print(base64.b64encode(os.urandom(32)).decode())'
ODIN_SECRET_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
