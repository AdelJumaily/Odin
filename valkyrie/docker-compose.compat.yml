version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["uvicorn", "apps.api.simple_app:app", "--host", "0.0.0.0", "--port", "6789"]
    ports:
      - "${API_HOST_PORT:-6789}:6789"
    volumes:
      - ./data:/app/data
    environment:
      VALKYRIE_STORAGE_PATH: /app/data/uploads
    env_file:
      - ./.env
